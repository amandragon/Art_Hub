<div class="index">

<h3>Welcome to the ArtHub Community!</h3>
<p>ArtHub is a new community of writers who want to see how they stack up. Join today, post your work, and get reviewed and scored by other writers like you.</p>

<h3>Sign up</h3>

  <div class="row">
    <div class="small-5">
      <%= form_for(@user) do |f| %>
        <% unless @user.errors.count == 0   %>
          <div class="alert alert-error">
            The form contains <%= pluralize(@user.errors.count, "error") %>.
          </div>
          <ul>
          <% @user.errors.full_messages.each do |msg| %>
            <li>* <%= msg %></li>
          <% end %>
          </ul>
        <% end %>

        <%= f.label :name %>
        <%= f.text_field :name %>

        <%= f.label :email %>
        <%= f.text_field :email %>

        <%= f.label :password %>
        <%= f.password_field :password %>

        <%= f.label :password_confirmation, "Confirmation" %>
        <%= f.password_field :password_confirmation %><br>

        <%= f.label :about_me %>
        <%= f.text_area :about_me, :cols => "30", :rows => "5" %>


        <!-- bootstrap for other fields in bio-->

        <%= f.submit "Create my account", class: "button" %>
      <% end %>
    </div>
  </div> 
</div>